{
  "name": "leadboard",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "LeadBoard — voice-assisted team lead cockpit",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --prefix fe",
    "dev": "npm run dev --prefix be",
    "dev:fe": "npm run dev --prefix fe",
    "reset": "npm run reset --prefix be"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0",
    "cors": "^2.8.5",
    "dayjs": "^1.11.13",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mongoose": "^8.9.5",
    "mongoose-lean-virtuals": "^2.1.0",
    "morgan": "^1.10.0",
    "multer": "^2.0.1",
    "zod": "^3.24.1"
  }
}
